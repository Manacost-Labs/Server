# design/components

Search the component registry and Storybook states before introducing primitives. Reuse existing Button, Card, Badge, Tabs, Tooltip, Modal, Dropdown, Table and project equivalents. Keep shared primitives free of product logic. Register variants with source, stories, screenshots and tokens. Retrieve only relevant entries.
