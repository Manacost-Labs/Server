# design/svg

Require viewBox, approved UI size, accessible labels at the component boundary and consistent currentColor semantics. Reject scripts, event attributes and external references. Optimize with project SVGO rules. Never blindly recolor multicolor artwork or overwrite originals.
