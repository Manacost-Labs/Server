# design/tokens

Use project semantic tokens for colors, spacing, typography, radius, shadows, containers, breakpoints and motion. Preserve the existing theme. Validate aliases, missing variables and duplicate declarations. Scope exceptions explicitly with reasons. Recheck consumers after token changes.
