# design/visual-regression

Use versioned baselines with stable browsers, fonts, viewports, data and animations. Compare selected routes and states. Preserve actual and diff artifacts. Never auto-accept baselines. Pixel checks complement keyboard, accessibility and computed styles.
