# engineering/nextjs-base

Use project formatting, lint and TypeScript checks. Preserve server/client and module boundaries. Run focused unit and relevant integration tests. Run unused-code and dependency checks when applicable. Scope browser checks to changed routes. Use configured security scans. Do not auto-install tools from verification commands.
