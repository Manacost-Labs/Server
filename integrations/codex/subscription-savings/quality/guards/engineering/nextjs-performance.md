# engineering/nextjs-performance

Measure selected routes and bundles against comparable baselines. Track JavaScript bytes, LCP, INP, CLS and latency where relevant. Keep server dependencies out of clients. Full performance/load tests require heavy verification and explicit targets. Record missing measurements; thresholds belong to project configuration.
