# token/retrieval

Select explicit source and test paths. Retrieve definitions, syntactic references and related tests before expanding scope. AST references are candidates, not resolved calls. Validate cache hashes against dirty files. Pack required evidence verbatim and deduplicate optional ranges. Report omissions and stay below 12000 estimated tokens. Escalate to docs, external references, embeddings or reranking only for a named evidence gap. Verify packets before reuse.
